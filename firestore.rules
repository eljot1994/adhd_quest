rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Zezwalaj na odczyt i zapis tylko zalogowanym użytkownikom
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}